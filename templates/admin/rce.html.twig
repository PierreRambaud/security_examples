{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}
  <div class="bd-example">
    <a href="{{ path('security_examples_rce_command', {type: 'router'}) }}" class="btn btn-outline-primary">Debug Router</a>
    <a href="{{ path('security_examples_rce_command', {type: 'form'}) }}" class="btn btn-outline-primary">Debug Form</a>
  </div>

  {% if output is not empty %}
    {{ output|nl2br }}
  {% endif %}
{% endblock %}
